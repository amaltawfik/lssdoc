# lssdoc 0.0.0.9000

* Initial package scaffolding: the user-facing API (`parse_lss()`,
  `audit_lss()`, `render_lss_docx()`, and the `lss_to_docx()` wrapper) is
  defined and documented, with implementations to follow.
