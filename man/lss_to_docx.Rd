% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lss_to_docx.R
\name{lss_to_docx}
\alias{lss_to_docx}
\title{Convert a \code{.lss} file to a Word review document}
\usage{
lss_to_docx(input, output, ...)
}
\arguments{
\item{input}{Path to a \code{.lss} file.}

\item{output}{Path to the \code{.docx} file to create.}

\item{...}{
  Arguments passed on to \code{\link{render_lss_docx}}
  \describe{
    \item{\code{languages}}{Character vector of language codes to display, \strong{in the
order they will appear as columns}. Use this both to subset (display
only the languages you want) and to order them; for example
\code{c("fr", "de")} puts French first, while \code{c("de", "fr")} puts German
first. The first language is treated as the primary language: the
question heading shown in the table of contents includes the question
text in that language, and group headings fall back to it. Requesting a
language absent from the survey is an error (\code{lssdoc_unknown_language}).
Defaults to all languages found in the \code{.lss} file, in the order of the
\verb{<languages>} section.}
    \item{\code{template}}{Output style. \code{"cards"} (default) renders one
detached pair of tables per item (a meta table + an item table)
stacked vertically, with the survey's content languages
displayed side-by-side in the item table. \code{"table"} renders a
single dense table covering the whole document in codebook
style: every variable is one row, the meta columns
(\code{No | Variable | Type | Mandatory | Filter}) come first, then
one column per content language carrying the question stem, the
subquestion label (when applicable), and the response modalities
stacked underneath. Group banners become merged section rows
inside the table; the column header repeats on every page
automatically. The \code{table} template auto-promotes the page
format to A4 landscape for 2+ languages (overridable through
\code{page_format}).}
    \item{\code{layout}}{Reserved for future use. Currently \code{"side-by-side"} only.}
    \item{\code{show_audit}}{Logical; include an audit summary section near the top
and inline markers on questions that carry findings.}
    \item{\code{show_help}}{Logical; include question help texts under the question
text.}
    \item{\code{show_attrs}}{Character vector of question attributes to display
under the question text when present.}
    \item{\code{show_technical_attrs}}{Logical; include technical attributes such as
\code{answer_order} and \verb{location_*}.}
    \item{\code{page_format}}{Page format. \code{"auto"} picks portrait for one or two
languages and landscape from three. Use \code{"A4-portrait"},
\code{"A4-landscape"}, or \code{"A3"} to force a layout.}
    \item{\code{show_toc}}{Logical; include a table of contents listing the groups
of the survey. Skipped automatically when the survey has fewer than
two groups (a single-group survey makes the TOC redundant). Items
themselves are not in the TOC; use \code{show_index} for a navigable
variable index.}
    \item{\code{show_index}}{Logical; append a variable index at the end of the
document listing every item code with its item number, sorted
alphabetically. Useful for cross-referencing a specific variable.}
    \item{\code{show_header_title}}{Logical; show the survey title at the top
right of every page, one line per displayed language. Long titles
are truncated to 80 characters with a trailing ellipsis. Default
\code{TRUE}. When \code{FALSE}, only the \code{X/Y} page counter shows at the
bottom right.}
    \item{\code{show_source}}{Logical; show the \strong{Source file} name and the
\strong{Survey ID} rows in the cover metadata table. Default \code{TRUE}
keeps them for traceability; pass \code{FALSE} to hide both (some
reviewers prefer not to expose the LimeSurvey survey id or the
internal filename).}
    \item{\code{show_item_heading}}{Logical; show a bold heading
\code{"N. variable"} above each item. Default \code{FALSE}: the meta table
starts each item directly, for a compact layout. Set to \code{TRUE} to
add the heading line for scroll-time navigation; the item number is
already present in the meta table's \code{No} column and in the variable
index so the heading is redundant for cross-reference purposes.}
    \item{\code{show_raw_filter}}{Logical; when \code{TRUE} (the default) the Filter
cell of each meta table shows the human-readable form on top and the
raw LimeSurvey relevance expression in smaller italic gray underneath.
Set to \code{FALSE} for a cleaner cell that shows only the plain form (the
raw expression is still shown when it could not be simplified).}
    \item{\code{show_groups}}{Logical; show the group banners (cards layout) or
group rows (table layout). Default \code{TRUE}. Surveys sometimes use
groups only as internal organization tools without showing them
to respondents; pass \code{FALSE} to flatten the document into a
single sequence of items with no section breaks.}
    \item{\code{show_welcome}}{Logical; include the survey's welcome text
(\code{surveyls_welcometext}) in the document. Default \code{TRUE}. The
welcome text is multilingual and appears as a side-by-side block
in the cards layout or as an embedded row in the codebook table.}
    \item{\code{show_endtext}}{Logical; include the survey's end text
(\code{surveyls_endtext}). Default \code{TRUE}. Same multilingual treatment
as \code{show_welcome}.}
    \item{\code{show_description}}{Logical; include the survey's description
(\code{surveyls_description}). Default \code{TRUE}. The description is the
short "what this survey is about" multilingual intro that
LimeSurvey shows above the welcome text on the landing page; it
appears here before the welcome block in the cards layout and as
a Description row in the codebook table.}
    \item{\code{show_privacy_settings}}{Logical; surface the survey-level
privacy / tracking settings (\code{anonymized}, \code{save} partial,
\code{datestamp}, \code{ipaddr}, \code{refurl}) as additional rows on the cover
metadata table. Default \code{FALSE} because most reviewers do not
need them; set to \code{TRUE} for an ethics committee submission
where these flags are part of the methodological assessment.}
    \item{\code{show_admin_settings}}{Logical; surface the survey-level
administrative settings (\code{alias}, end URL and its description,
\code{active} flag) as additional rows on the cover metadata table.
Default \code{FALSE}. The end URL is where the respondent is
redirected after submission; reviewers may want to check it for
third-party redirects.}
    \item{\code{title}}{Optional override of the survey title shown on the cover
page and the top-right header. \code{NULL} (default) uses the per-
language titles from the \code{.lss} survey settings. Pass a single
string to use the same title in every displayed language, or a
named character vector keyed by language code (e.g.
\code{c(fr = "Mon titre", de = "Mein Titel")}) for per-language overrides.}
    \item{\code{logo}}{Optional path to an image (PNG or JPEG) to display at the top
of the cover page. The \code{.lss} file does not embed a logo, so this image
must be supplied by the caller. \code{NULL} (default) keeps the cover
logo-free, matching the neutral style of survey-methodology references
(ESS, MOSAiCH, Panel).}
    \item{\code{logo_width,logo_height}}{Image dimensions in inches. Defaults are
tuned to a 2:1 logo (1.5 x 0.75 inches). Resize or pre-crop your image
to fit if it has a different aspect ratio.}
    \item{\code{font}}{Body font name. \code{NULL} (default) keeps Calibri, which is
pre-installed on every recent Windows Office and metric-substituted
with Carlito (OFL) on Mac and Linux LibreOffice, so column widths
stay stable across platforms. Pass a string to override
(e.g. \code{"Source Sans 3"}, \code{"IBM Plex Sans"}, or a corporate brand
font). The string is passed through to Word; install the font on
the machine that opens the document, otherwise the reader's
application will substitute its own fallback face.}
    \item{\code{font_code}}{Monospace font used for code-like content: the
variable column in each meta table, the raw relevance expression
under each filter cell, and the variable index entries. \code{NULL}
(default) keeps Consolas; pass \code{"JetBrains Mono"} or \code{"IBM Plex Mono"} for sharper code style if installed on the reader's
machine.}
    \item{\code{colors}}{Optional named list of hex color overrides for the
editorial petrol-blue palette. \code{NULL} (default) keeps the
package palette intact. Accepted names: \code{"primary"} (group
filets, headers text and item top borders, 1.0 pt strokes),
\code{"accent"} (hyperlinks, ORCID iD, URL auto-links, group
under-line in the cards layout), \code{"band"} (light header
backgrounds in the meta table headers and the codebook's
Question rows), \code{"band_dark"} (the meta-table dark header in
the cards layout), \code{"zebra"} (the very-light tint on the
codebook Question rows in the table template), \code{"grid"}
(the 0.5 pt border color used for every internal table line),
\code{"text"} and \code{"muted"}. Each value must be a hex string
(\code{"#XXXXXX"} or \code{"#XXX"}). Unknown keys are rejected with a
classed condition so a typo never silently turns into a
no-op. Useful for honoring an institutional brand: e.g.
\code{colors = list(primary = "#5C9F1A", accent = "#7FA82E")}
produces a LimeSurvey-green codebook instead of the default
petrol blue.}
    \item{\code{chrome_lang}}{Language used for the \strong{chrome} of the document
(column headers, row labels, navigation titles, MOSAiCH-style
type labels, Value descriptors, audit section). Independent of
\code{languages}, which controls the survey's content columns.
Supported values: \code{"en"}, \code{"fr"}, \code{"de"}, \code{"es"}, \code{"it"}.
\code{NULL} (default) follows \code{languages[1]} when it is a supported
chrome language, otherwise falls back to \code{"en"}. Pass the value
explicitly to force a specific chrome language regardless of the
content -- e.g. \code{chrome_lang = "en"} with \code{languages = c("fr", "de")} produces an English-labeled document with French and
German survey content. Spanish and Italian translations should
be reviewed by a native speaker before publishing an official
document.}
    \item{\code{description}}{Optional free-form text shown on the cover page
below the authors block. Useful for a citation hint, a funding
acknowledgement, a methodology note, or a link to a related
publication. Pass a single string; line breaks (\verb{\\n}) split the
block into separate centered lines. Any \verb{http://} or \verb{https://}
token is rendered as a clickable hyperlink, so DOI URLs or article
permalinks become navigable. Plain text otherwise.}
    \item{\code{authors}}{Optional credit block for the questionnaire's
designers, displayed on the cover page below the subtitle. Each
author is shown centered on its own line as \verb{Name \\u2014 Affiliation};
when an ORCID iD is provided, a smaller monospace line below
shows \verb{ORCID 0000-0000-0000-0000} as a hyperlink to
\verb{https://orcid.org/<id>}. Accepts:
\itemize{
\item \code{NULL} (default): no authorship block.
\item An \strong{unnamed character vector} (\code{c("Amal Tawfik", "John Doe")}):
each entry becomes a line with no affiliation.
\item A \strong{named character vector} (\code{c("Amal Tawfik" = "HES-SO Valais")}):
names are authors, values are affiliations. Use the empty
string \code{""} to render an author without affiliation.
\item A \strong{list of named lists} for the full form, e.g.
\code{list(list(name = "Amal Tawfik", affiliation = "HES-SO Valais", orcid = "0009-0006-2422-1555"), list(name = "John Doe", affiliation = "UNIL"))}. The \code{name} field is required; the
\code{affiliation} and \code{orcid} fields are optional.
}}
  }}
}
\value{
The \code{output} path, invisibly.
}
\description{
One-shot pipeline: parse the LimeSurvey \code{.lss} file with \code{\link[=parse_lss]{parse_lss()}}
and render it to a \code{.docx} review document with \code{\link[=render_lss_docx]{render_lss_docx()}}. Use
the underlying functions directly when you need finer control over the
audit or the layout.
}
\examples{
\dontrun{
lss_to_docx(
  system.file("extdata", "hesav_2026.lss", package = "lssdoc"),
  "rapport.docx"
)
}
}
