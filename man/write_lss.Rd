% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_lss.R
\name{write_lss}
\alias{write_lss}
\title{Write a survey specification to an importable \code{.lss} file}
\usage{
write_lss(spec, file, sid = 100001L, settings = list())
}
\arguments{
\item{spec}{An \code{\link[=lss_spec]{lss_spec()}} object, or a plain list with the same
structure (it is then validated through \code{lss_spec()} first).}

\item{file}{Character. Path of the \code{.lss} file to write.}

\item{sid}{Integer. Survey id embedded in the file. LimeSurvey assigns
a fresh id on import when this one is taken, so the value rarely
matters.}

\item{settings}{Named list of \code{surveys}-table fields overriding the
built-in defaults (e.g. \code{list(anonymized = "Y", showprogress = "Y")}).
The defaults ship with the package and come from a real LimeSurvey 6
export, scrubbed -- see \code{lss_default_surveys_fields} in the sources
for the rationale.}
}
\value{
Invisibly, the path to the written file.
}
\description{
\strong{Experimental.} Turn an \code{\link[=lss_spec]{lss_spec()}} specification into a
LimeSurvey 6 structure file (\code{.lss}, DBVersion 700) that imports
directly through \emph{Create survey -> Import}. The emitted file can be
read back with \code{\link[=read_lss]{read_lss()}}, checked with \code{\link[=audit_lss]{audit_lss()}} and rendered
with \code{\link[=render_questionnaire]{render_questionnaire()}} -- so the document reviewers read is
produced from the very file LimeSurvey receives.
}
\details{
Mapping choices, each validated against real LimeSurvey 6 imports:
\itemize{
\item Each kind maps to a LimeSurvey type and theme attested by a corpus
of real exports (see \code{lss_kind_map} in the sources). Options of
single-choice lists, rankings and array columns are emitted as
\code{answers}; options of multiple-choice questions, item batteries and
array rows as \code{subquestions}; scalar kinds and implicit scales
(yes/no, gender, five-point, 5/10-point arrays) emit none.
\item The native \code{other} option is emitted as \code{other = "Y"} plus the
localized attribute \code{other_replace_text}. Localized attributes MUST
carry the language code: emitted without one, LimeSurvey silently
ignores them and shows its default wording. Global attributes
(\code{exclude_all_others}, \code{max_answers}, ...) stay language-less.
\item \code{other_position} / \code{other_position_code} control where the other
option is displayed; \code{exclude_all_others} accepts several codes
separated by \verb{;}.
\item Relevance equations are translated from the minimal syntax of
\code{\link[=lss_spec]{lss_spec()}} into ExpressionScript (\code{code.NAOK == "1"}).
\item Quotas are emitted with limit zero and the terminate action.
\item A mandatory or capped ranking also receives \code{min_answers = 1},
overridable through the question's \code{attributes}.
}
}
\examples{
spec <- lss_spec(
  title = "Demo",
  groups = list(list(title = "G", questions = list(
    list(code = "q1", kind = "single", text = "Oui ou non ?",
         options = list(list(text = "Oui"), list(text = "Non")))
  )))
)
out <- tempfile(fileext = ".lss")
write_lss(spec, out)
audit_lss(out)
}
\seealso{
\code{\link[=lss_spec]{lss_spec()}}, \code{\link[=read_lss]{read_lss()}}, \code{\link[=audit_lss]{audit_lss()}},
\code{\link[=render_questionnaire]{render_questionnaire()}}.
}
