% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_lss_audit_docx.R
\name{render_lss_audit_docx}
\alias{render_lss_audit_docx}
\title{Render the audit alone as a focused Word document}
\usage{
render_lss_audit_docx(
  lss,
  output,
  languages = NULL,
  logo = NULL,
  logo_width = 1.5,
  logo_height = 0.75,
  font = NULL,
  font_code = NULL,
  colors = NULL,
  authors = NULL,
  description = NULL,
  chrome_lang = NULL
)
}
\arguments{
\item{lss}{An \code{lss} object returned by \code{\link[=parse_lss]{parse_lss()}}.}

\item{output}{Path to the \code{.docx} file to create.}

\item{languages}{Character vector of language codes for the cover page.
Defaults to all languages of the survey.}

\item{logo}{Optional path to a PNG or JPEG image to display at the top
of the cover page. \code{NULL} (default) keeps the cover logo-free.}

\item{logo_width, logo_height}{Image dimensions in inches. Defaults
tuned to a 2:1 logo (1.5 x 0.75 inches).}

\item{font}{Body font name. \code{NULL} (default) keeps Calibri. See
\code{\link[=render_lss_docx]{render_lss_docx()}} for guidance on overrides.}

\item{font_code}{Monospace font used for code-like content
(variable codes, raw expressions). \code{NULL} (default) keeps Consolas.}

\item{colors}{Optional named list of hex color overrides. Same
shape and accepted names as in \code{\link[=render_lss_docx]{render_lss_docx()}}.}

\item{authors, description}{Optional cover-page credit block and
free-form note. Same shapes as in \code{\link[=render_lss_docx]{render_lss_docx()}}.}

\item{chrome_lang}{Language used for the document chrome (column
headers, row labels, audit section). Supported: \code{"en"}, \code{"fr"},
\code{"de"}, \code{"es"}, \code{"it"}. \code{NULL} (default) follows \code{languages[1]}
when supported, otherwise \code{"en"}.}
}
\value{
The \code{output} path, invisibly.
}
\description{
Build a short, action-oriented \code{.docx} that contains only the audit
findings: the same cover page as the full review document, the summary
counts, then one table per severity (errors, warnings, notes) listing
every finding with its location and message. Use this for QA follow-up
or to share the issues with a colleague without distributing the full
questionnaire.
}
\examples{
\dontrun{
lss <- parse_lss(system.file("extdata", "limesurvey_survey_751689.lss",
  package = "lssdoc"
))
render_lss_audit_docx(lss, tempfile(fileext = ".docx"))
}
}
