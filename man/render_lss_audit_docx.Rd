% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_lss_audit_docx.R
\name{render_lss_audit_docx}
\alias{render_lss_audit_docx}
\title{Render the audit alone as a focused Word document}
\usage{
render_lss_audit_docx(lss, output, languages = NULL)
}
\arguments{
\item{lss}{An \code{lss} object returned by \code{\link[=parse_lss]{parse_lss()}}.}

\item{output}{Path to the \code{.docx} file to create.}

\item{languages}{Character vector of language codes for the cover page.
Defaults to all languages of the survey.}
}
\value{
The \code{output} path, invisibly.
}
\description{
Build a short, action-oriented \code{.docx} that contains only the audit
findings: the same cover page as the full review document, the summary
counts, then one table per severity (errors, warnings, notes) listing
every finding with its location and message. Use this for QA follow-up
or to share the issues with a colleague without distributing the full
questionnaire.
}
\examples{
\dontrun{
lss <- parse_lss(system.file("extdata", "limesurvey_survey_751689.lss",
  package = "lssdoc"
))
render_lss_audit_docx(lss, tempfile(fileext = ".docx"))
}
}
